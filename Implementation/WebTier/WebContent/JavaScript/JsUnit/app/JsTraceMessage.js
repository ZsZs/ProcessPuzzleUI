var JsTraceMessage = new Class( {
   initialize : function( message, value, traceLevel ) {
      this.message = message;
      this.value = value;
      this.traceLevel = traceLevel;
   },
});
